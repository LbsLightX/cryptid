{
  "name": "website-n-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "deploy": "pnpm run build && rsync -a --delete --progress ./dist/ aa@pi:/compose/beta-site/files && rm -rf ./dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.1.2"
  }
}
